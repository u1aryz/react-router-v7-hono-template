pre-commit:
  commands:
    check:
      run: npx biome check --write --no-errors-on-unmatched --files-ignore-unknown=true {staged_files} && npm run typecheck
      stage_fixed: true

commit-msg:
  commands:
    "lint commit message":
      run: npx --no -- commitlint --edit "$1"
